// a) Apresentar os quadrados dos números inteiros de 15 a 200.

let quadrado;
let resultado;

alert("Vamos apresentar os quadrados dos números inteiros de 15 a 200");

resultado

for (quadrado = 15; quadrado <= 200; quadrado++) {
  resultado = (quadrado * quadrado);
  alert(`O quadrado de ${quadrado} é ${resultado}`);
}
